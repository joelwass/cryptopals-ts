If you also wish to estimate theIf you also wish actual likelihood of the message being English (or, rather, the likelihood that picking random letters with frequencies matching English text could yield such a message), you'll also need to determine the number of degrees of freedom k for the model (which, for a simple model like this, is simply the size of the plaintext alphabet minus on(which, for a simple model like this, is simply the size of the plaintext alphabet minus on